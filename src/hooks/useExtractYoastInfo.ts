const useExtractYoastInfo = (yoastJson) => {
  console.log({ yoastJson })
  const { canonical, og_locale, og_site_name, og_title } = yoastJson;
  // return {

  // }
}

export default useExtractYoastInfo
